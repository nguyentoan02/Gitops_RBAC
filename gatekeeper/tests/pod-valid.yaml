apiVersion: v1
kind: Pod
metadata:
  name: test-valid-pod
  namespace: demo
spec:
  containers:
  - name: nginx
    image: nginx:1.27.0
    securityContext:
      runAsUser: 1000
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
